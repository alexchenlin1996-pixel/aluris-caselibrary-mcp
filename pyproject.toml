[project]
name = "aluris-caselibrary-mcp"
version = "0.1.0"
description = "司法案例库 MCP Server — 最高法指导案例/公报案例/案例库 语义检索"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "numpy>=1.26",
    "httpx>=0.27",
    "jieba>=0.42",
    "playwright>=1.60.0",
    "beautifulsoup4>=4.15.0",
]

[project.scripts]
case-library-mcp = "server:main"

[tool.uv]
dev-dependencies = []
